# Copy this file to .env for local development (see README.md).
# Must be prefixed with PUBLIC_ to be readable by Astro's client-side script —
# note that also means it ships in the page's JS bundle and is NOT a secret.
PUBLIC_MEMBERS_PASSWORD=
